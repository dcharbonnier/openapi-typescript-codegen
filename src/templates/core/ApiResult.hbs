{{>header}}

export type ApiResult = {
	readonly url: string;
	readonly ok: boolean;
	readonly status: number;
	readonly requestId?: string | null;
	readonly statusText: string;
	readonly body: any;
};
